title: Python Project
plan:
- title: Welcome
  statement: markdowns/welcome.md
projects:
  python:
    root: /python-project
    runner:
      name: python
      version: '3'
  angular:
    root: /vuejs-project # Set the root path to your angular project
    # Set the docker image. This image runs npm install for you on every build
    runner:
      name: zimdo/docker-vuejs
      version: latest
